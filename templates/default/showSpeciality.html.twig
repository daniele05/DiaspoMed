{% extends 'base.html.twig' %}

{% block title %} ici ma Page  {% endblock %}

{% block body %}

    <div class="container text-center">
        <div class="row">

            <div class="col align-self-start mt-3">
                <div>
                <img src="{{asset(speciality.specialityImage) }}" alt="Speciality Image" class="w-75 h-25 pb-4">
                </div>
            </div>
            <div class="col align-self-center">
                <h4 class="text-info">{{speciality.specialityName}}</h4>
                <p class="text-justify">{{ speciality.specialityContent }}</p>
            </div>
        </div>
    </div>


{% endblock %}