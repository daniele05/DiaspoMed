<!--navbar -->

<nav class="navbar navbar-expand-lg bg-body-tertiary">
    <div class="container-fluid">
        <a class="navbar-brand" href="#">
            <img src="{{ asset('assets/images/logo/logoUmla.png')}}" alt="image " class="img-fluid logo">
        </a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor04" aria-controls="navbarColor04" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse " id="navbarColor04">
            <ul class="navbar-nav  mr-auto mx-5">
                <li class="nav-item ml-5 ">
                    <a class="nav-link text-info active" href="{{ path('home') }}">Accueil</a>
                </li>
                <li class="nav-item  ml-5">
                    <a class="nav-link text-info" href="{{ path('app_default_presentation') }}">A propos</a>
                </li>

                <li class="nav-item dropdown">
                    <a class="nav-link text-info dropdown-toggle" data-bs-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">Nos Services</a>
                    <div class="dropdown-menu">
                        <a class="dropdown-item text-info" href="{{ path('app_default_cardspeciality') }}"> Spécialités</a>
                        <a class="dropdown-item text-info" href="{{ path('app_default_doctors') }}">Médécins</a>
                        <a class="dropdown-item text-info" href="{{ path('app_default_consultation') }}">Contact & Consultation</a>
                        <a class="dropdown-item text-info" href="{{ path('app_default_posologie') }}">Posologie</a>
                        <div class="dropdown-divider"></div>
                        {% if app.user %}
                            <a class="dropdown-item text-info" href="{{ path('app_logout')}}">Déconnexion</a>
                        {% else %}
                            <a class="dropdown-item text-info" href="{{ path('app_login') }}">Connexion</a>

                            <a class="dropdown-item text-info" href="#">Inscription</a>
                                <div>
                                    <ul>
                                        <li><a class="text-dark text-decoration-none" href="{{ path('app_user_registerdoctor') }}">Inscription Docteur</a></li>
                                        <li><a class="text-dark text-decoration-none" href="{{ path('app_user_registerpatient') }}">Inscription Patient</a></li>
                                    </ul>
                                </div>

                        {% endif %}
                    </div>
                </li>
            </ul>
            <div class="d-flex m-3" >
                <a href="{{ path('app_appointment_new' , { 'id' : app.user.id}) }}">Prendre rdv</a>
            </div>
        </div>
    </div>
</nav>