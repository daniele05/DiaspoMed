<nav class="navbar navbar-expand-lg bg-body-tertiary mb-5">
    <div class="container-fluid">
        <a class="navbar-brand" href="{{ path('home') }}">
            <img src="{{ asset('assets/images/logo/logoUmla.png') }}" alt="Logo" width="30" height="24"
                 class="d-inline-block align-text-top">
            Umla
        </a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavDropdown"
                aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="container">
            <div class="collapse navbar-collapse" id="navbarNavDropdown">
                <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('home') }}">Accueil</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_default_presentation') }}">A propos</a>
                    </li>
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown"
                           aria-expanded="false">
                            Nos services
                        </a>
                        <ul class="dropdown-menu">
                            <a class="dropdown-item" href="{{ path('app_default_cardspeciality') }}"> Spécialités</a>
                            <a class="dropdown-item" href="{{ path('app_default_doctors') }}">Médécins</a>
                            <a class="dropdown-item" href="{{ path('app_default_consultation') }}">Contact &
                                Consultation</a>
                            <a class="dropdown-item" href="{{ path('app_default_posologie') }}">Posologie</a>
                            <div class="dropdown-divider"></div>
                            {% if app.user %}
                                <a class="dropdown-item" href="{{ path('app_logout') }}">Déconnexion</a>
                            {% else %}
                                <a class="dropdown-item" href="{{ path('app_login') }}">Connexion</a>

                                <a class="dropdown-item" href="#">Inscription</a>
                                <div>
                                    <ul>
                                        <li><a class="text-dark text-decoration-none"
                                               href="{{ path('app_user_registerdoctor') }}">Inscription Docteur</a></li>
                                        <li><a class="text-dark text-decoration-none"
                                               href="{{ path('app_user_registerpatient') }}">Inscription Patient</a>
                                        </li>
                                    </ul>
                                </div>
                            {% endif %}
                        </ul>
                    </li>
                </ul>
                <div class="d-flex">
                    <ul class="navbar-nav ">
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('app_appointment_new') }}">
                                <i class="fa fa-user-doctor"></i>
                                Consultation en ligne
                            </a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('app_appointment_new') }}">
                                <i class="fa fa-sign-in"></i>
                                Connexion
                            </a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('app_appointment_new') }}">
                                <i class="fa fa-user-plus"></i>
                                Inscription
                            </a>
                        </li>
                    </ul>
                </div>
            </div>
        </div>
    </div>
</nav>
