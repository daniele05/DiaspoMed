{% extends 'base.html.twig' %}

{% block title %}Rdv{% endblock %}

{% block body %}
    <h3>Rdv</h3>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ appointment.id }}</td>
            </tr>
            <tr>
                <th>Place</th>
                <td>{{ appointment.place }}</td>
            </tr>
            <tr>
                <th>ScheduledDate</th>
                <td>{{ appointment.scheduledDate ? appointment.scheduledDate|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_appointment_index') }}">Retour à ma liste Rdv </a>

    <a href="{{ path('app_appointment_edit', {'id': appointment.id}) }}">Modifier</a>

    {{ include('appointment/_delete_form.html.twig') }}
{% endblock %}
